import {assert} from "chai";
import "mocha";
import Renderer from "../../../src/model/pdf/Renderer";

describe("Renderer", () => {

    it.skip("render should generate a pdf", async () => {
        // const renderer = new Renderer();
        // await renderer.createPdf();
        assert.isTrue(true);
    });

});
